# Skill Seekers 中文使用指南

**版本:** v2.0.0 | **更新时间:** 2025年11月

[![版本](https://img.shields.io/badge/版本-2.0.0-blue.svg)](https://github.com/yusufkaraaslan/Skill_Seekers/releases/tag/v2.0.0)
[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![PyPI](https://badge.fury.io/py/skill-seekers.svg)](https://pypi.org/project/skill-seekers/)

---

## 📖 目录

1. [项目简介](#项目简介)
2. [核心功能](#核心功能)
3. [快速开始](#快速开始)
4. [安装指南](#安装指南)
5. [基础使用](#基础使用)
6. [高级功能](#高级功能)
7. [配置文件详解](#配置文件详解)
8. [常见问题](#常见问题)
9. [实用技巧](#实用技巧)
10. [故障排查](#故障排查)

---

## 项目简介

### 什么是 Skill Seekers?

Skill Seekers 是一款强大的自动化工具,能够将任何文档网站、GitHub 代码库、PDF 文件转换为 Claude AI 可用的技能包。无需手动整理文档,几分钟内即可创建专业级的 AI 知识库。

### 主要特点

- 🌐 **通用文档抓取** - 支持任何文档网站
- 🐙 **GitHub 集成** - 深度分析代码仓库
- 📄 **PDF 支持** - 提取 PDF 文档内容
- 🔄 **多源统一** - 合并文档+代码+PDF (v2.0 新功能)
- ⚡ **快速高效** - 异步模式速度提升 2-3 倍
- 🤖 **AI 增强** - 自动生成高质量技能文件
- 📦 **即用即得** - 已发布到 PyPI,一键安装

### 适用场景

- 🎯 **开发者** - 创建框架/API 的快速参考
- 🎮 **游戏开发** - 游戏引擎文档(Godot、Unity 等)
- 🔧 **团队协作** - 将内部文档转为 AI 知识库
- 📚 **学习者** - 构建完整的学习资料包
- 🔍 **开源项目** - 分析代码与文档的差异

---

## 核心功能

### 1. 📚 文档抓取

- ✅ **llms.txt 优先** - 自动检测并使用 LLM 优化文档(10倍速度)
- ✅ **智能分类** - 自动将内容分类整理
- ✅ **代码识别** - 自动识别 Python、JavaScript、C++、GDScript 等语言
- ✅ **24+ 预设配置** - 开箱即用的热门框架配置

### 2. 🐙 GitHub 仓库抓取 (v2.0)

- ✅ **深度代码分析** - AST 解析支持多种语言
- ✅ **API 提取** - 自动提取函数、类、方法签名
- ✅ **仓库元数据** - README、文件树、语言统计
- ✅ **Issues & PR** - 获取开放/关闭的问题
- ✅ **变更日志** - 自动提取版本历史
- ✅ **冲突检测** - 对比文档与实际代码

### 3. 📄 PDF 支持 (v1.2.0)

- ✅ **基础提取** - 文本、代码、图片
- ✅ **OCR 扫描** - 支持扫描版 PDF
- ✅ **密码保护** - 处理加密 PDF
- ✅ **表格提取** - 提取复杂表格
- ✅ **并行处理** - 大文件快 3 倍

### 4. 🔄 多源统一抓取 (v2.0 新功能)

**问题**: 文档和代码经常不同步。文档可能过时,缺少实际存在的功能,或记录了已删除的特性。

**解决方案**: 统一抓取功能:
- 从多个来源(文档+GitHub+PDF)提取信息
- **自动检测冲突** - 文档与代码之间的差异
- **智能合并** - 基于规则或 AI 的冲突解决
- **透明报告** - 并排显示差异,带 ⚠️ 警告
- **单一真相源** - 一个技能同时显示意图(文档)和现实(代码)

**冲突类型**:
- 🔴 **代码中缺失** (高) - 已记录但未实现
- 🟡 **文档中缺失** (中) - 已实现但未记录
- ⚠️ **签名不匹配** - 参数/类型不同
- ℹ️ **描述不匹配** - 说明不同

### 5. 🤖 AI 增强

- ✅ **本地增强(推荐)** - 免费使用 Claude Code Max,无需 API 密钥
- ✅ **API 增强(备选)** - 使用 Anthropic API(约 $0.15-0.30/技能)
- ✅ **质量提升** - 将 75 行模板转换为 500+ 行专业指南
- ✅ **智能提取** - 自动选择最佳代码示例

### 6. ⚡ 性能优化

- ✅ **异步模式** - 2-3 倍速度提升
- ✅ **大型文档支持** - 处理 10K-40K+ 页文档
- ✅ **路由技能** - 自动将大文档拆分为专业子技能
- ✅ **并行抓取** - 同时处理多个任务
- ✅ **检查点/恢复** - 长时间抓取不丢失进度
- ✅ **缓存系统** - 抓取一次,立即重建

---

## 快速开始

### 最快方式: PyPI 安装

```bash
# 1. 从 PyPI 安装(最简单!)
pip install skill-seekers

# 2. 立即使用统一 CLI
skill-seekers scrape --config configs/react.json
skill-seekers --help
```

**用时:** 3 分钟

### 完整工作流程

```bash
# 1. 预估页数(可选,1-2 分钟)
skill-seekers estimate configs/godot.json

# 2. 抓取文档并使用本地增强(无需 API 密钥)
skill-seekers scrape --config configs/godot.json --enhance-local
# 用时: 20-40 分钟抓取 + 60 秒增强

# 3. 打包技能
skill-seekers package output/godot/

# 完成! godot.zip 已准备好上传到 Claude
```

---

## 安装指南

### 前置要求

1. **Python 3.10+** - [下载](https://www.python.org/downloads/) | 检查: `python3 --version`
2. **Git** - [下载](https://git-scm.com/) | 检查: `git --version`
3. **15-30 分钟** 首次设置时间

### 方式 1: PyPI 安装(推荐)

```bash
# 全局或虚拟环境安装
pip install skill-seekers

# 立即使用统一 CLI
skill-seekers scrape --config configs/react.json
skill-seekers --help
```

### 方式 2: 从源码安装(开发用)

```bash
# 克隆仓库
git clone https://github.com/yusufkaraaslan/Skill_Seekers.git
cd Skill_Seekers

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# Windows: venv\Scripts\activate

# 可编辑模式安装
pip install -e .

# 或手动安装依赖
pip install -r requirements.txt
```

### 方式 3: 使用 uv(现代工具)

```bash
# 使用 uv 安装
uv tool install skill-seekers

# 或直接运行(无需安装)
uv tool run --from skill-seekers skill-seekers scrape --config https://raw.githubusercontent.com/yusufkaraaslan/Skill_Seekers/main/configs/react.json
```

### 方式 4: MCP 集成(Claude Code)

```bash
# 一次性设置(5 分钟)
./setup_mcp.sh

# 然后在 Claude Code 中,直接用自然语言:
"从 https://react.dev/ 生成 React 技能"
"抓取 docs/manual.pdf 并创建技能"
```

---

## 基础使用

### 1. 使用预设配置

最简单的方式是使用已有的预设配置:

```bash
# Godot 游戏引擎
skill-seekers scrape --config configs/godot.json

# React 框架
skill-seekers scrape --config configs/react.json

# Vue.js 框架
skill-seekers scrape --config configs/vue.json

# Django Web 框架
skill-seekers scrape --config configs/django.json

# FastAPI
skill-seekers scrape --config configs/fastapi.json

# 查看所有预设
ls configs/
```

**可用预设(24 个)**:
- **Web 框架**: react, vue, astro, django, laravel, fastapi, hono
- **DevOps**: ansible-core, kubernetes
- **游戏引擎**: godot, godot-large-example
- **CSS**: tailwind
- **游戏**: steam-economy-complete
- **开发工具**: claude-code

### 2. 交互模式

回答几个问题,自动创建配置:

```bash
skill-seekers scrape --interactive
```

系统会问你:
- 技能名称?
- 文档 URL?
- 描述(何时使用)?
- 最大页数?

### 3. 快速模式

一条命令快速创建技能:

```bash
skill-seekers scrape \
  --name react \
  --url https://react.dev/ \
  --description "React 框架用于构建 UI"
```

### 4. 预估页数

在正式抓取前,先预估页数:

```bash
# 快速预估 - 发现最多 100 页
skill-seekers estimate configs/react.json --max-discovery 100
# 用时: 30-60 秒

# 完整预估 - 发现最多 1000 页(默认)
skill-seekers estimate configs/godot.json
# 用时: 1-2 分钟

# 深度预估 - 发现最多 2000 页
skill-seekers estimate configs/vue.json --max-discovery 2000
# 用时: 3-5 分钟
```

**为什么要预估**:
- 在完整抓取前验证配置的 URL 模式
- 帮助设置最佳 `max_pages` 值
- 估算总抓取时间
- 快速(仅 HEAD 请求+最小解析)
- 不下载或存储数据

### 5. 使用缓存数据

如果已经抓取过,可以直接使用缓存重建:

```bash
# 首次抓取
skill-seekers scrape --config configs/react.json

# 之后提示时选择使用现有数据
✓ 发现现有数据: 245 页
使用现有数据? (y/n): y

# 或使用 --skip-scrape 标志
skill-seekers scrape --config configs/react.json --skip-scrape
# 用时: 1-3 分钟(即时重建)
```

### 6. 异步模式(2-3倍速度)

启用异步模式大幅提升速度:

```bash
# 推荐 8 个工作线程
skill-seekers scrape --config configs/react.json --async --workers 8

# 快速模式 + 异步
skill-seekers scrape --name react --url https://react.dev/ --async --workers 8

# 试运行 + 异步
skill-seekers scrape --config configs/godot.json --async --workers 4 --dry-run
```

**推荐设置**:
- 小文档(~100-500 页): `--async --workers 4`
- 中文档(~500-2000 页): `--async --workers 8`
- 大文档(2000+ 页): `--async --workers 8 --no-rate-limit`

**性能对比**:
- 同步: ~18 页/秒, 120 MB 内存
- 异步: ~55 页/秒, 40 MB 内存(快 3 倍!)

---

## 高级功能

### 1. PDF 提取

```bash
# 基础 PDF 提取
skill-seekers pdf --pdf docs/manual.pdf --name myskill

# 高级功能
skill-seekers pdf --pdf docs/manual.pdf --name myskill \
    --extract-tables \        # 提取表格
    --parallel \              # 快速并行处理
    --workers 8               # 使用 8 个 CPU 核心

# 扫描版 PDF(需要: pip install pytesseract Pillow)
skill-seekers pdf --pdf docs/scanned.pdf --name myskill --ocr

# 密码保护的 PDF
skill-seekers pdf --pdf docs/encrypted.pdf --name myskill --password mypassword
```

**用时:** 5-15 分钟(或使用并行处理 2-5 分钟)

### 2. GitHub 仓库抓取

从 GitHub 仓库创建技能，提取代码结构、API 签名、Issues、CHANGELOG 等内容。

#### 快速开始

**最简单的方式（只需仓库地址）:**

```bash
# 格式: skill-seekers github --repo owner/repo
skill-seekers github --repo facebook/react
skill-seekers github --repo django/django
skill-seekers github --repo godotengine/godot
```

> **💡 提示:** `facebook/react` 是 GitHub 仓库的标准简写格式，等同于完整 URL `https://github.com/facebook/react`。命令中只需使用 `owner/repo` 格式即可，无需输入完整 URL。

**完整工作流程（从仓库地址到技能包）:**

```bash
# 步骤 1: 抓取 GitHub 仓库
skill-seekers github --repo facebook/react

# 步骤 2: 打包技能
skill-seekers package output/react/

# 步骤 3: 上传到 Claude
# - 手动上传: 将 output/react.zip 上传到 https://claude.ai/skills
# - 或自动上传: skill-seekers upload output/react.zip
```

**用时:** 5-10 分钟 | **输出:** 完整的代码库技能包

---

#### 获取 GitHub Token (推荐但可选)

**为什么需要 Token?**
- 未认证: 每小时 60 次 API 请求
- 已认证: 每小时 5000 次 API 请求 (83倍提升!)

**如何获取:**

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选权限:
   - ✅ `public_repo` (读取公共仓库)
   - ✅ `repo` (如需访问私有仓库)
4. 生成并复制 token (如: `ghp_xxxxxxxxxxxx`)
5. 设置环境变量:

```bash
# Linux/macOS (临时)
export GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Linux/macOS (永久 - 添加到 ~/.bashrc 或 ~/.zshrc)
echo 'export GITHUB_TOKEN=ghp_xxxxxxxxxxxx' >> ~/.bashrc
source ~/.bashrc

# Windows (临时 - CMD)
set GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Windows (临时 - PowerShell)
$env:GITHUB_TOKEN="ghp_xxxxxxxxxxxx"

# Windows (永久)
setx GITHUB_TOKEN "ghp_xxxxxxxxxxxx"
```

**验证 Token:**

```bash
# 检查是否设置成功
echo $GITHUB_TOKEN  # Linux/macOS
echo %GITHUB_TOKEN%  # Windows CMD
echo $env:GITHUB_TOKEN  # Windows PowerShell

# 测试 Token (应返回你的 GitHub 用户信息)
curl -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/user
```

---

#### 基础用法

**1. 快速抓取 (最小配置)**

```bash
# 只需仓库地址
skill-seekers github --repo facebook/react

# 自动包含:
# - README.md
# - 仓库信息 (stars, forks, 语言统计)
# - 文件树结构
# - 主要编程语言
```

**2. 使用配置文件**

```bash
# 使用预设配置
skill-seekers github --config configs/react_github.json

# 或创建自定义配置 (见下文)
skill-seekers github --config configs/myrepo_github.json
```

**3. 完整功能抓取**

```bash
skill-seekers github --repo django/django \
    --include-issues \        # 包含 GitHub Issues
    --max-issues 100 \        # 限制 issue 数量
    --include-changelog \     # 包含 CHANGELOG.md
    --include-releases        # 包含 GitHub Releases
```

**4. 深度代码分析**

```bash
skill-seekers github --repo facebook/react \
    --include-code \                    # 启用代码分析
    --code-analysis-depth deep \        # 分析深度: surface/deep/full
    --file-patterns "packages/**/*.js"  # 只分析匹配的文件
```

---

#### 完整参数说明

| 参数 | 类型 | 说明 | 默认值 | 示例 |
|------|------|------|--------|------|
| `--repo` | 字符串 | **必需** GitHub 仓库 (owner/repo) | - | `facebook/react` |
| `--config` | 文件路径 | 配置文件路径 | - | `configs/react_github.json` |
| `--name` | 字符串 | 技能名称 | 仓库名 | `react` |
| `--description` | 字符串 | 技能描述 | 自动生成 | `"React 库"` |
| `--include-issues` | 布尔 | 包含 GitHub Issues | `true` | `--include-issues` |
| `--max-issues` | 数字 | 最大 issue 数量 | `100` | `--max-issues 200` |
| `--include-changelog` | 布尔 | 包含 CHANGELOG | `true` | `--include-changelog` |
| `--include-releases` | 布尔 | 包含 Releases | `true` | `--include-releases` |
| `--include-code` | 布尔 | 启用代码分析 | `false` | `--include-code` |
| `--code-analysis-depth` | 字符串 | 分析深度 | `surface` | `deep` / `full` |
| `--file-patterns` | 字符串 | 文件匹配模式 | `[]` | `"src/**/*.py"` |

**分析深度说明:**
- **surface** (表面): 基础信息 - README, 文件树, 语言统计 (快速, 1-2 分钟)
- **deep** (深入): + 函数/类签名, 文档字符串 (中速, 3-5 分钟)
- **full** (完整): + AST 解析, 类型推断, 调用图 (慢速, 10-15 分钟)

---

#### 抓取内容说明

**自动提取的内容:**

1. **仓库信息**
   - 仓库名称、描述
   - Stars、Forks、Watchers 数量
   - 创建/更新时间
   - 主要编程语言

2. **README 文档**
   - README.md (完整内容)
   - README 中的代码示例
   - 徽章和链接

3. **文件树结构**
   - 完整目录结构
   - 文件类型统计
   - 关键文件识别 (setup.py, package.json, etc.)

4. **语言统计**
   - 按字节数统计每种语言
   - 百分比分布

5. **代码分析** (如果启用 `--include-code`)
   - 函数/类签名
   - 参数和返回类型
   - 文档字符串
   - 代码示例

6. **GitHub Issues** (如果启用 `--include-issues`)
   - 标题和描述
   - 状态 (open/closed)
   - 标签 (labels)
   - 里程碑 (milestones)

7. **CHANGELOG** (如果启用 `--include-changelog`)
   - CHANGELOG.md 完整内容
   - 版本历史

8. **Releases** (如果启用 `--include-releases`)
   - 版本号
   - 发布日期
   - 发布说明

---

#### 配置文件创建

**创建 GitHub 抓取配置文件:**

```bash
# 创建配置文件
cat > configs/myrepo_github.json << 'EOF'
{
  "name": "myrepo",
  "repo": "owner/myrepo",
  "description": "My awesome repository skill",
  "github_token": null,
  "include_issues": true,
  "max_issues": 100,
  "include_changelog": true,
  "include_releases": true,
  "include_code": false,
  "code_analysis_depth": "surface",
  "file_patterns": []
}
EOF

# 使用配置文件
skill-seekers github --config configs/myrepo_github.json
```

**配置文件模板说明:**

```json
{
  "name": "技能名称",
  "repo": "owner/repo",  // GitHub 仓库 (必需)
  "description": "技能描述",
  "github_token": null,  // 不推荐在配置文件中存储 token,使用环境变量

  // 可选内容
  "include_issues": true,       // 是否包含 Issues
  "max_issues": 100,           // 最大 issue 数量
  "include_changelog": true,   // 是否包含 CHANGELOG
  "include_releases": true,    // 是否包含 Releases

  // 代码分析 (高级)
  "include_code": false,       // 是否启用代码分析
  "code_analysis_depth": "surface",  // 分析深度: surface/deep/full
  "file_patterns": [           // 文件匹配模式 (glob)
    "src/**/*.py",
    "lib/**/*.js"
  ]
}
```

---

#### 实际示例

**示例 1: React 库 (快速)**

```bash
# 基础抓取
skill-seekers github --repo facebook/react

# 输出:
# - README.md (使用说明)
# - 文件树 (packages/, scripts/, fixtures/)
# - 语言统计 (JavaScript 98.5%, HTML 1.2%, etc.)
# - 仓库信息 (200K+ stars)

# 用时: 2-3 分钟
```

**示例 2: Django 框架 (包含 Issues)**

```bash
# 使用认证 + 包含 Issues
export GITHUB_TOKEN=ghp_xxxxxxxxxxxx
skill-seekers github --repo django/django \
    --include-issues \
    --max-issues 200

# 输出:
# - 基础内容 + 200 个最新 Issues
# - Issue 标签 (bug, enhancement, etc.)
# - Issue 里程碑

# 用时: 5-7 分钟
```

**示例 3: Godot 引擎 (深度代码分析)**

```bash
# 深度分析 C++ 和 GDScript 代码
skill-seekers github --repo godotengine/godot \
    --include-code \
    --code-analysis-depth deep \
    --file-patterns "core/**/*.cpp" "modules/**/*.cpp"

# 输出:
# - 基础内容
# - C++ 函数/类签名
# - 参数类型和文档
# - 代码示例

# 用时: 10-15 分钟
```

**示例 4: FastAPI (完整抓取)**

```bash
# 包含所有可能的内容
skill-seekers github --repo tiangolo/fastapi \
    --include-issues \
    --max-issues 50 \
    --include-changelog \
    --include-releases \
    --include-code \
    --code-analysis-depth deep \
    --file-patterns "fastapi/**/*.py"

# 输出:
# - README + 文件树 + 语言统计
# - 50 个最新 Issues
# - CHANGELOG.md
# - 所有 GitHub Releases
# - Python 代码分析 (函数签名, 类型提示)

# 用时: 8-12 分钟
```

---

#### 输出结构

抓取完成后的文件结构:

```
output/
├── react_github_data.json  # 原始抓取数据 (JSON)
└── react/                   # 技能目录
    ├── SKILL.md            # 主技能文件
    ├── references/         # 参考文档
    │   ├── index.md        # 仓库概览
    │   ├── readme.md       # README 内容
    │   ├── structure.md    # 文件树结构
    │   ├── api.md          # API 签名 (如果启用代码分析)
    │   ├── issues.md       # Issues 列表 (如果包含)
    │   ├── changelog.md    # CHANGELOG (如果包含)
    │   └── releases.md     # Releases (如果包含)
    ├── scripts/            # 空 (用户脚本)
    └── assets/             # 空 (用户资产)
```

---

#### 高级用法

**1. 分析特定文件类型**

```bash
# 只分析 Python 文件
skill-seekers github --repo requests/requests \
    --include-code \
    --file-patterns "requests/**/*.py" "tests/**/*.py"

# 只分析 TypeScript 文件
skill-seekers github --repo microsoft/vscode \
    --include-code \
    --file-patterns "src/**/*.ts"
```

**2. 私有仓库抓取**

```bash
# 需要 token 且具有私有仓库权限
export GITHUB_TOKEN=ghp_xxxxxxxxxxxx
skill-seekers github --repo mycompany/private-repo
```

**3. 限制速率避免限制**

```bash
# 添加延迟避免速率限制
skill-seekers github --repo large/repo --rate-limit 2.0
```

**4. 组合文档和代码**

```bash
# 先抓取文档
skill-seekers scrape --config configs/fastapi.json

# 再抓取 GitHub (会合并到同一技能)
skill-seekers github --repo tiangolo/fastapi

# 或使用统一配置 (推荐)
skill-seekers unified --config configs/fastapi_unified.json
```

---

#### 常见问题

**Q1: 速率限制错误 (403: rate limit exceeded)**

```
GitHub API rate limit exceeded
```

**解决方案:**
1. 设置 GITHUB_TOKEN 环境变量 (提升到 5000 次/小时)
2. 等待 1 小时后重试
3. 检查速率限制状态:
   ```bash
   curl https://api.github.com/rate_limit
   ```

**Q2: 仓库不存在或无权限**

```
Repository not found: owner/repo
```

**解决方案:**
1. 检查仓库名称拼写
2. 确认仓库是公开的,或者你有访问权限
3. 对于私有仓库,确保 token 有 `repo` 权限

**Q3: 抓取速度慢**

**解决方案:**
1. 减少 max_issues: `--max-issues 50`
2. 禁用不需要的内容: `--no-issues --no-releases`
3. 减少代码分析深度: `--code-analysis-depth surface`
4. 使用文件模式限制范围: `--file-patterns "src/**/*.py"`

**Q4: 代码分析失败**

```
Code analysis failed for file: xxx.py
```

**解决方案:**
1. 语法错误的文件会被跳过 (正常)
2. 减少分析深度: `surface` 代替 `deep`
3. 使用文件模式排除问题文件
4. 查看日志了解详细错误

**Q5: 如何更新已有技能?**

```bash
# 删除旧数据
rm -rf output/react_github_data.json output/react/

# 重新抓取
skill-seekers github --repo facebook/react
```

---

#### 性能和限制

| 操作 | 未认证 | 已认证 | 说明 |
|------|--------|--------|------|
| API 速率限制 | 60/小时 | 5000/小时 | 建议使用 token |
| 基础抓取 | 2-3 分钟 | 2-3 分钟 | README + 基础信息 |
| + Issues (100) | +2 分钟 | +1 分钟 | 每 30 个 issue 1 次请求 |
| + Releases | +1 分钟 | +30 秒 | 通常较少 |
| + 代码分析 (surface) | +2 分钟 | +2 分钟 | 不受 API 限制 |
| + 代码分析 (deep) | +5 分钟 | +5 分钟 | 本地处理 |
| + 代码分析 (full) | +10 分钟 | +10 分钟 | 大型仓库更慢 |

**建议:**
- ✅ 始终使用 GITHUB_TOKEN
- ✅ 仅在需要时启用深度分析
- ✅ 使用文件模式限制范围
- ✅ 先用 surface 测试,再用 deep

---

**用时总结:**
- **快速** (基础): 2-3 分钟
- **标准** (+ Issues): 5-7 分钟
- **完整** (+ 所有内容 + 深度分析): 10-15 分钟

### 3. 多源统一抓取(v2.0 新功能)

**问题**: 文档和代码经常不一致。文档可能过时,缺少代码中存在的功能,或记录了已删除的功能。

**解决方案**: 将文档 + GitHub + PDF 合并为一个统一技能,显示差异并标注冲突。

```bash
# 使用现有统一配置
skill-seekers unified --config configs/react_unified.json
skill-seekers unified --config configs/django_unified.json

# 或创建统一配置(混合文档 + GitHub)
cat > configs/myframework_unified.json << 'EOF'
{
  "name": "myframework",
  "description": "从文档+代码获取完整框架知识",
  "merge_mode": "rule-based",
  "sources": [
    {
      "type": "documentation",
      "base_url": "https://docs.myframework.com/",
      "extract_api": true,
      "max_pages": 200
    },
    {
      "type": "github",
      "repo": "owner/myframework",
      "include_code": true,
      "code_analysis_depth": "surface"
    }
  ]
}
EOF

# 运行统一抓取器
skill-seekers unified --config configs/myframework_unified.json

# 打包并上传
skill-seekers package output/myframework/
# 将 output/myframework.zip 上传到 Claude - 完成!
```

**用时:** 30-45 分钟 | **质量:** 带冲突检测的生产级

**特殊之处**:

✅ **冲突检测** - 自动发现 4 种差异:
- 🔴 **代码中缺失**(高): 已记录但未实现
- 🟡 **文档中缺失**(中): 已实现但未记录
- ⚠️ **签名不匹配**: 参数/类型不同
- ℹ️ **描述不匹配**: 说明不同

✅ **透明报告** - 并排显示两个版本:
```markdown
#### `move_local_x(delta: float)`

⚠️ **冲突**: 文档签名与实现不同

**文档说:**
```
def move_local_x(delta: float)
```

**代码实现:**
```python
def move_local_x(delta: float, snap: bool = False) -> None
```
```

✅ **优势**:
- **识别文档差距** - 自动发现过时或缺失的文档
- **捕获代码变更** - 了解 API 何时更改但文档未更新
- **单一真相源** - 一个技能显示意图(文档)和现实(代码)
- **可操作见解** - 获取修复每个冲突的建议
- **开发辅助** - 查看代码库中实际存在的内容 vs 文档记录的内容

**完整指南:** 见 [docs/UNIFIED_SCRAPING.md](docs/UNIFIED_SCRAPING.md)

### 4. AI 增强

大幅提升 SKILL.md 质量:

```bash
# 方式 1: 抓取时增强(本地,无 API 密钥 - 推荐)
skill-seekers scrape --config configs/react.json --enhance-local

# 方式 2: 抓取时增强(API,需要 API 密钥)
pip3 install anthropic
export ANTHROPIC_API_KEY=sk-ant-...
skill-seekers scrape --config configs/react.json --enhance

# 方式 3: 抓取后独立增强(本地)
skill-seekers enhance output/react/

# 方式 4: 抓取后独立增强(API)
skill-seekers enhance output/react/ --api-key sk-ant-...
```

**本地增强(推荐)**:
- 使用你的 Claude Code Max 计划(无 API 成本)
- 打开带有 Claude Code 的新终端
- 自动分析参考文件
- 用时 30-60 秒
- 质量: 9/10(与 API 版本相当)

**增强效果**:
- 读取参考文档文件
- 使用 Claude 分析内容
- 提取 5-10 个最佳代码示例
- 创建全面的快速参考
- 添加领域特定的关键概念
- 为不同技能水平提供导航指导
- 自动备份原始文件
- **质量**: 将 75 行模板转换为 500+ 行专业指南

### 5. 大型文档支持(10K-40K+ 页)

处理超大文档站点(如 Godot 40K 页、AWS、Microsoft Docs):

```bash
# 1. 先预估(发现页数)
skill-seekers estimate configs/godot.json

# 2. 自动拆分为专注的子技能
python3 -m skill_seekers.cli.split_config configs/godot.json --strategy router

# 创建:
# - godot-scripting.json (5K 页)
# - godot-2d.json (8K 页)
# - godot-3d.json (10K 页)
# - godot-physics.json (6K 页)
# - godot-shaders.json (11K 页)

# 3. 并行抓取所有(4-8 小时而不是 20-40!)
for config in configs/godot-*.json; do
  skill-seekers scrape --config $config &
done
wait

# 4. 生成智能路由/中心技能
python3 -m skill_seekers.cli.generate_router configs/godot-*.json

# 5. 打包所有技能
python3 -m skill_seekers.cli.package_multi output/godot*/

# 6. 将所有 .zip 文件上传到 Claude
# 用户只需自然提问!
# 路由器自动引导到正确的子技能!
```

**拆分策略**:
- **auto** - 根据页数智能检测最佳策略
- **category** - 按文档类别拆分(脚本、2d、3d 等)
- **router** - 创建中心技能+专业子技能(推荐)
- **size** - 每 N 页拆分(用于无明确分类的文档)

**优势**:
- ✅ 更快抓取(并行执行)
- ✅ 更专注的技能(更好的 Claude 性能)
- ✅ 更易维护(一次更新一个主题)
- ✅ 自然的用户体验(路由器处理路由)
- ✅ 避免上下文窗口限制

**完整指南:** [docs/LARGE_DOCUMENTATION.md](docs/LARGE_DOCUMENTATION.md)

### 6. 检查点/恢复

长时间抓取永不丢失进度:

```bash
# 在配置中启用
{
  "checkpoint": {
    "enabled": true,
    "interval": 1000  // 每 1000 页保存
  }
}

# 如果抓取中断(Ctrl+C 或崩溃)
skill-seekers scrape --config configs/godot.json --resume

# 从最后一个检查点恢复
✅ 从检查点恢复(已抓取 12,450 页)
⏭️  跳过已抓取的 12,450 页
🔄 从中断处继续...

# 重新开始(清除检查点)
skill-seekers scrape --config configs/godot.json --fresh
```

**优势**:
- ✅ 自动保存每 1000 页(可配置)
- ✅ 中断时保存(Ctrl+C)
- ✅ 使用 `--resume` 标志恢复
- ✅ 永不丢失数小时的抓取进度

---

## 配置文件详解

### configs 目录说明

`configs/` 目录是存放所有配置文件的地方。每个配置文件定义了如何抓取特定文档网站的规则。

**目录位置**: `Skill_Seekers/configs/`

**文件格式**: JSON 格式 (`.json` 扩展名)

**作用**:
- 🎯 定义抓取的目标网站
- 📋 指定内容提取规则
- 🏷️ 设置分类和过滤规则
- ⚙️ 配置抓取参数(速率、页数等)

### 可用的预设配置文件

项目提供了 **24 个预设配置文件**,开箱即用:

#### Web 框架 (7 个)
- `react.json` - React 框架
- `vue.json` - Vue.js 框架
- `astro.json` - Astro 框架
- `django.json` - Django Web 框架
- `laravel.json` - Laravel 框架
- `fastapi.json` - FastAPI 框架
- `hono.json` - Hono Web 框架

#### DevOps & 自动化 (2 个)
- `ansible-core.json` - Ansible Core 2.19
- `kubernetes.json` - Kubernetes

#### 游戏引擎 (2 个)
- `godot.json` - Godot 引擎
- `godot-large-example.json` - Godot 大型文档示例

#### CSS & 工具 (2 个)
- `tailwind.json` - Tailwind CSS
- `claude-code.json` - Claude Code 文档

#### 其他 (1 个)
- `steam-economy-complete.json` - Steam 经济系统

#### 多源统一配置 (5 个) - v2.0 新功能
- `react_unified.json` - React (文档 + GitHub)
- `django_unified.json` - Django (文档 + GitHub)
- `fastapi_unified.json` - FastAPI (文档 + GitHub)
- `godot_unified.json` - Godot (文档 + GitHub)
- `fastapi_unified_test.json` - FastAPI 测试配置

#### 测试/示例配置 (5 个)
- `godot_github.json` - GitHub 单独抓取示例
- `react_github.json` - GitHub 单独抓取示例
- `python-tutorial-test.json` - Python 教程测试
- `example_pdf.json` - PDF 提取示例
- `test-manual.json` - 手动测试配置

### 配置文件示例: react.json

这是 React 的完整配置文件内容:

```json
{
  "name": "react",
  "description": "React framework for building user interfaces. Use for React components, hooks, state management, JSX, and modern frontend development.",
  "base_url": "https://react.dev/",
  "start_urls": [
    "https://react.dev/learn",
    "https://react.dev/learn/quick-start",
    "https://react.dev/learn/thinking-in-react",
    "https://react.dev/reference/react",
    "https://react.dev/reference/react-dom",
    "https://react.dev/reference/react/hooks"
  ],
  "selectors": {
    "main_content": "article",
    "title": "h1",
    "code_blocks": "pre code"
  },
  "url_patterns": {
    "include": ["/learn", "/reference"],
    "exclude": ["/community", "/blog"]
  },
  "categories": {
    "getting_started": ["quick-start", "installation", "tutorial"],
    "hooks": ["usestate", "useeffect", "usememo", "usecallback", "usecontext", "useref", "hook"],
    "components": ["component", "props", "jsx"],
    "state": ["state", "context", "reducer"],
    "api": ["api", "reference"]
  },
  "rate_limit": 0.5,
  "max_pages": 300
}
```

**配置文件各部分说明**:

#### 1. 基本信息
```json
{
  "name": "react",                    // 技能名称(用于输出目录)
  "description": "React framework...", // 技能描述(Claude 何时使用)
  "base_url": "https://react.dev/"    // 基础 URL(抓取起点)
}
```

#### 2. 起始 URL (可选)
```json
{
  "start_urls": [
    "https://react.dev/learn",           // 从这些页面开始抓取
    "https://react.dev/reference/react"  // 可以指定多个起点
  ]
}
```

#### 3. CSS 选择器
```json
{
  "selectors": {
    "main_content": "article",  // 主内容区域选择器
    "title": "h1",              // 标题选择器
    "code_blocks": "pre code"   // 代码块选择器
  }
}
```

**常见选择器值**:
- `main_content`: `"article"`, `"main"`, `"div[role='main']"`, `"div.content"`
- `title`: `"h1"`, `"title"`, `"h1.page-title"`
- `code_blocks`: `"pre code"`, `"pre"`, `"div.highlight"`

#### 4. URL 过滤规则
```json
{
  "url_patterns": {
    "include": ["/learn", "/reference"],  // 只抓取包含这些路径的页面
    "exclude": ["/community", "/blog"]    // 排除这些路径
  }
}
```

#### 5. 内容分类
```json
{
  "categories": {
    "getting_started": ["quick-start", "installation", "tutorial"],
    "hooks": ["usestate", "useeffect", "hook"],
    "api": ["api", "reference"]
  }
}
```

工具会根据 URL、标题、内容中的关键词自动分类页面。

#### 6. 抓取参数
```json
{
  "rate_limit": 0.5,  // 请求间隔(秒) - 避免被封
  "max_pages": 300    // 最大抓取页数
}
```

### 如何使用预设配置

```bash
# 方式 1: 直接使用
skill-seekers scrape --config configs/react.json

# 方式 2: 查看所有可用配置
ls configs/

# 方式 3: 查看配置内容
cat configs/react.json

# 方式 4: 测试配置(限制页数)
skill-seekers scrape --config configs/react.json --max-pages 20
```

### 基本配置结构

如果要创建自己的配置文件,可以参考这个模板:

```json
{
  "name": "myframework",
  "description": "何时使用此技能",
  "base_url": "https://docs.myframework.com/",
  "selectors": {
    "main_content": "article",
    "title": "h1",
    "code_blocks": "pre code"
  },
  "url_patterns": {
    "include": ["/docs", "/guide"],
    "exclude": ["/blog", "/about"]
  },
  "categories": {
    "getting_started": ["intro", "quickstart"],
    "api": ["api", "reference"]
  },
  "rate_limit": 0.5,
  "max_pages": 500
}
```

### 参数说明

| 参数 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | 字符串 | 技能标识符 | `"react"`, `"godot"` |
| `description` | 字符串 | Claude 何时使用此技能 | `"React 框架用于构建 UI"` |
| `base_url` | 字符串 | 抓取起始 URL | `"https://react.dev/"` |
| `selectors.main_content` | 字符串 | 主内容 CSS 选择器 | `"article"`, `"div[role='main']"` |
| `selectors.title` | 字符串 | 页面标题选择器 | `"h1"`, `"title"` |
| `selectors.code_blocks` | 字符串 | 代码块选择器 | `"pre code"`, `"pre"` |
| `url_patterns.include` | 数组 | 只抓取包含这些模式的 URL | `["/docs", "/api"]` |
| `url_patterns.exclude` | 数组 | 跳过包含这些模式的 URL | `["/blog", "/_static/"]` |
| `categories` | 对象 | 关键词分类映射 | 见下文 |
| `rate_limit` | 数字 | 请求间延迟(秒) | `0.5`, `1.0` |
| `max_pages` | 数字 | 最大抓取页数 | `500`, `2000` |

### 分类配置

```json
{
  "categories": {
    "getting_started": ["introduction", "getting_started", "quickstart"],
    "scripting": ["scripting", "gdscript", "code"],
    "api": ["api", "reference", "class"],
    "tutorials": ["tutorial", "example", "guide"]
  }
}
```

系统会根据 URL、标题和内容中的关键词自动分类页面。

### 统一配置(多源)

```json
{
  "name": "react",
  "description": "从文档+代码库获取完整 React 知识",
  "merge_mode": "rule-based",
  "sources": [
    {
      "type": "documentation",
      "base_url": "https://react.dev/",
      "extract_api": true,
      "max_pages": 200
    },
    {
      "type": "github",
      "repo": "facebook/react",
      "include_code": true,
      "code_analysis_depth": "surface"
    }
  ]
}
```

---

## 打包和上传

### 方式 1: 自动上传(API)

```bash
# 设置 API 密钥(一次性)
export ANTHROPIC_API_KEY=sk-ant-...

# 打包并自动上传
skill-seekers package output/react/ --upload

# 或上传现有 .zip
skill-seekers upload output/react.zip
```

**优势**:
- ✅ 完全自动
- ✅ 无需手动步骤
- ✅ 从命令行工作

**要求**:
- Anthropic API 密钥(从 https://console.anthropic.com/ 获取)

### 方式 2: 手动上传(无 API 密钥)

```bash
# 打包技能
skill-seekers package output/react/

# 这将:
# 1. 创建 output/react.zip
# 2. 自动打开 output/ 文件夹
# 3. 显示上传说明

# 然后手动上传:
# - 访问 https://claude.ai/skills
# - 点击"上传技能"
# - 选择 output/react.zip
# - 完成!
```

**优势**:
- ✅ 不需要 API 密钥
- ✅ 适用于所有人
- ✅ 文件夹自动打开

### 方式 3: Claude Code(MCP) - 智能自动

```
在 Claude Code 中,直接问:
"打包并上传 React 技能"

# 如果设置了 API 密钥:
# - 打包技能
# - 自动上传到 Claude
# - 完成! ✅

# 如果没有 API 密钥:
# - 打包技能
# - 显示 .zip 位置
# - 提供手动上传说明
```

**优势**:
- ✅ 自然语言
- ✅ 智能自动检测(如有 API 密钥可用则上传)
- ✅ 有无 API 密钥都能工作
- ✅ 无错误或失败

---

## 实用技巧

### 1. 先小范围测试

编辑配置文件以限制页数进行测试:

```json
{
  "max_pages": 20  // 仅测试 20 页
}
```

### 2. 复用抓取数据

```bash
# 抓取一次
skill-seekers scrape --config configs/react.json

# 多次重建(即时)
skill-seekers scrape --config configs/react.json --skip-scrape
skill-seekers scrape --config configs/react.json --skip-scrape
```

### 3. 查找正确的选择器

使用浏览器开发工具或 Python 测试:

```python
from bs4 import BeautifulSoup
import requests

url = "https://docs.example.com/page"
soup = BeautifulSoup(requests.get(url).content, 'html.parser')

# 尝试不同的选择器
print(soup.select_one('article'))
print(soup.select_one('main'))
print(soup.select_one('div[role="main"]'))
print(soup.select_one('div.content'))

# 测试代码块选择器
print(soup.select('pre code'))
print(soup.select('pre'))
```

### 4. 检查输出质量

构建后验证质量:

```bash
# 检查 SKILL.md 是否有真实示例
cat output/godot/SKILL.md

# 检查分类结构
cat output/godot/references/index.md

# 列出所有参考文件
ls output/godot/references/

# 检查特定分类内容
cat output/godot/references/getting_started.md

# 验证代码示例有语言检测
grep -A 3 "```" output/godot/references/*.md | head -20
```

### 5. 创建自定义配置

```bash
# 复制预设
cp configs/react.json configs/myframework.json

# 编辑它
nano configs/myframework.json

# 使用它
skill-seekers scrape --config configs/myframework.json
```

---

## 常见问题

### Q1: 抓取速度慢怎么办?

**A:** 使用异步模式:
```bash
skill-seekers scrape --config configs/react.json --async --workers 8
```

### Q2: 如何更新过时的技能?

**A:** 删除旧数据并重新抓取:
```bash
rm -rf output/godot_data/
skill-seekers scrape --config configs/godot.json
```

### Q3: 能处理需要登录的网站吗?

**A:** 目前不支持需要认证的网站。但可以:
- 使用 GitHub 抓取器获取公共代码仓库
- 使用 PDF 抓取器处理下载的文档

### Q4: 如何减少抓取的页数?

**A:** 编辑配置文件:
```json
{
  "max_pages": 100,  // 减少页数
  "url_patterns": {
    "include": ["/docs/api"],  // 更具体的包含模式
    "exclude": ["/blog", "/community"]  // 排除不需要的部分
  }
}
```

### Q5: 为什么没有提取到内容?

**A:** 检查 CSS 选择器。常见选择器:
- `article`
- `main`
- `div[role="main"]`
- `div.content`
- `div#content`

使用浏览器开发工具查看实际的 HTML 结构。

### Q6: 支持中文文档吗?

**A:** 完全支持!工具能处理任何 UTF-8 编码的文档,包括中文、日文、韩文等。

### Q7: 生成的技能文件有多大?

**A:** 取决于文档大小:
- 小文档(50-100 页): 1-5 MB
- 中文档(200-500 页): 5-20 MB
- 大文档(1000+ 页): 20-100 MB

### Q8: 可以同时抓取多个网站吗?

**A:** 可以!使用统一配置或并行运行:
```bash
# 方式 1: 统一配置(推荐)
skill-seekers unified --config configs/myframework_unified.json

# 方式 2: 并行运行
skill-seekers scrape --config configs/react.json &
skill-seekers scrape --config configs/vue.json &
wait
```

---

## 故障排查

### Python 未找到

**错误:**
```
python3: command not found
```

**解决方案:**
1. 检查是否安装:
   ```bash
   which python3
   python --version  # 尝试不带 3
   ```

2. 安装 Python:
   - **macOS:** `brew install python3`
   - **Linux:** `sudo apt install python3 python3-pip`
   - **Windows:** 从 python.org 下载,勾选"Add to PATH"

3. 尝试使用 python 代替 python3:
   ```bash
   python cli/doc_scraper.py --help
   ```

### 模块未找到

**错误:**
```
ModuleNotFoundError: No module named 'requests'
```

**解决方案:**
```bash
# 安装依赖
pip3 install requests beautifulsoup4

# 如果权限被拒绝,使用 --user
pip3 install --user requests beautifulsoup4

# 或使用虚拟环境(推荐)
python3 -m venv venv
source venv/bin/activate
pip install requests beautifulsoup4
```

### 未提取到内容

**问题:** 页面已抓取但内容为空

**解决方案:**
1. 检查配置中的 `main_content` 选择器
2. 尝试: `article`, `main`, `div[role="main"]`, `div.content`
3. 使用上面的 Python 代码测试选择器

### 分类不佳

**问题:** 页面分类不准确

**解决方案:**
1. 编辑配置文件的 `categories` 部分
2. 添加更具体的关键词
3. 查看抓取的 URL 模式:
   ```bash
   cat output/godot_data/summary.json | grep url | head -20
   ```

### 速率限制/429 错误

**错误:**
```
HTTP Error 429: Too Many Requests
```

**解决方案:**
1. 增加 `rate_limit` 值:
   ```json
   {
     "rate_limit": 2.0  // 从 0.5 增加到 2.0 秒
   }
   ```

2. 减少 `max_pages`:
   ```json
   {
     "max_pages": 50  // 抓取更少页面
   }
   ```

3. 稍后重试

### 权限被拒绝

**错误:**
```
Permission denied: '/usr/local/lib/python3.x/...'
```

**解决方案:**
```bash
# 使用 --user 标志
pip3 install --user requests beautifulsoup4

# 或使用虚拟环境(最佳实践)
python3 -m venv venv
source venv/bin/activate
pip install requests beautifulsoup4
```

### MCP 服务器未加载

**症状:**
- 工具未出现在 Claude Code 中
- "列出所有可用配置"不起作用

**解决方案:**
1. 检查配置文件:
   ```bash
   cat ~/.config/claude-code/mcp.json
   ```

2. 验证路径是绝对路径(不是占位符):
   ```json
   {
     "mcpServers": {
       "skill-seeker": {
         "args": [
           "/Users/yourname/Projects/Skill_Seekers/mcp/server.py"
         ]
       }
     }
   }
   ```
   ❌ 错误: `$REPO_PATH` 或 `/path/to/Skill_Seekers`
   ✅ 正确: `/Users/john/Projects/Skill_Seekers`

3. 手动测试服务器:
   ```bash
   cd ~/Projects/Skill_Seekers
   python3 mcp/server.py
   # 应该无错误启动(Ctrl+C 停止)
   ```

4. 重新运行设置脚本:
   ```bash
   ./setup_mcp.sh
   # 选择"y"自动配置
   ```

5. **完全重启 Claude Code**(不仅是关闭窗口)

---

## 性能对比

| 任务 | 时间 | 说明 |
|------|------|------|
| 抓取(同步) | 15-45 分钟 | 仅首次,基于线程 |
| 抓取(异步) | 5-15 分钟 | 使用 --async 标志快 2-3 倍 |
| 构建 | 1-3 分钟 | 快速! |
| 重建 | <1 分钟 | 使用 --skip-scrape |
| 增强(本地) | 30-60 秒 | 使用 Claude Code Max |
| 增强(API) | 20-40 秒 | 需要 API 密钥 |
| 打包 | 5-10 秒 | 最终 zip |

---

## 输出结构

```
output/
├── godot_data/              # 抓取的原始数据(缓存)
│   ├── pages/              # 单独的页面 JSON
│   ├── summary.json        # 抓取摘要
│   └── checkpoint.json     # 恢复检查点
│
└── godot/                   # 技能目录
    ├── SKILL.md            # 主技能文件
    ├── SKILL.md.backup     # 备份(如果增强)
    ├── references/         # 分类文档
    │   ├── index.md
    │   ├── getting_started.md
    │   ├── api.md
    │   └── ...
    ├── scripts/            # 空(用户脚本)
    └── assets/             # 空(用户资产)
```

---

## 完整工作流程示例

### 场景 1: 首次创建(含抓取+增强)

```bash
# 1. 抓取 + 构建 + AI 增强(本地,无 API 密钥)
skill-seekers scrape --config configs/godot.json --enhance-local

# 2. 等待增强终端关闭(约 60 秒)

# 3. 验证质量
cat output/godot/SKILL.md

# 4. 打包
skill-seekers package output/godot/

# 完成!godot.zip 已准备好上传到 Claude
```

**用时:** 20-40 分钟(抓取) + 60 秒(增强) = 约 21-41 分钟

### 场景 2: 使用现有数据(快速!)

```bash
# 1. 使用现有数据 + 本地增强
skill-seekers scrape --config configs/godot.json --skip-scrape
skill-seekers enhance output/godot/

# 2. 打包
skill-seekers package output/godot/

# 完成!
```

**用时:** 1-3 分钟(构建) + 60 秒(增强) = 约 2-4 分钟

### 场景 3: 无增强(基础)

```bash
# 1. 抓取 + 构建(无增强)
skill-seekers scrape --config configs/godot.json

# 2. 打包
skill-seekers package output/godot/

# 完成!(SKILL.md 将是基础模板)
```

**用时:** 20-40 分钟
**注意:** SKILL.md 将是通用的 - 强烈推荐增强!

### 场景 4: 大型文档(40K 页)

```bash
# 1. 预估页数(快速,1-2 分钟)
skill-seekers estimate configs/godot.json

# 2. 拆分为专注的子技能
python3 -m skill_seekers.cli.split_config configs/godot.json --strategy router --target-pages 5000

# 创建: godot-scripting.json, godot-2d.json, godot-3d.json, 等

# 3. 并行抓取所有(4-8 小时而不是 20-40!)
for config in configs/godot-*.json; do
  skill-seekers scrape --config $config &
done
wait

# 4. 生成智能路由技能
python3 -m skill_seekers.cli.generate_router configs/godot-*.json

# 5. 打包所有技能
python3 -m skill_seekers.cli.package_multi output/godot*/

# 6. 将所有 .zip 文件上传到 Claude
# 结果: 路由器自动将查询引导到正确的子技能!
```

**时间节省:** 并行抓取将 20-40 小时减少到 4-8 小时

---

## 验证命令

使用这些命令检查设置:

```bash
# 1. 检查 Python
python3 --version  # 应该是 3.10+

# 2. 检查依赖
pip3 list | grep requests
pip3 list | grep beautifulsoup4
pip3 list | grep mcp

# 3. 检查文件存在
ls cli/doc_scraper.py
ls mcp/server.py
ls configs/

# 4. 检查 MCP 配置
cat ~/.config/claude-code/mcp.json

# 5. 测试抓取器
skill-seekers scrape --help

# 6. 测试 MCP 服务器
timeout 3 python3 mcp/server.py || echo "服务器正常"

# 7. 检查 git 仓库
git status
git log --oneline -5
```

---

## 获取帮助

如果这些解决方案都不起作用:

1. **查看现有问题:**
   https://github.com/yusufkaraaslan/Skill_Seekers/issues

2. **开启新问题,包含:**
   - 你的操作系统(macOS 13、Ubuntu 22.04 等)
   - Python 版本(`python3 --version`)
   - 完整错误信息
   - 你运行的命令
   - 上述验证命令的输出

3. **包含此调试信息:**
   ```bash
   # 系统信息
   uname -a
   python3 --version
   pip3 --version

   # Skill Seeker 信息
   cd ~/Projects/Skill_Seekers  # 你的路径
   pwd
   git log --oneline -1
   ls -la cli/ mcp/ configs/

   # MCP 配置(如果使用 MCP)
   cat ~/.config/claude-code/mcp.json
   ```

---

## 文档资源

### 入门指南
- **[BULLETPROOF_QUICKSTART.md](BULLETPROOF_QUICKSTART.md)** - 🎯 新手从这里开始!
- **[QUICKSTART.md](QUICKSTART.md)** - 有经验用户的快速入门
- **[TROUBLESHOOTING.md](TROUBLESHOOTING.md)** - 常见问题和解决方案

### 指南
- **[docs/LARGE_DOCUMENTATION.md](docs/LARGE_DOCUMENTATION.md)** - 处理 10K-40K+ 页文档
- **[ASYNC_SUPPORT.md](ASYNC_SUPPORT.md)** - 异步模式指南(抓取快 2-3 倍)
- **[docs/ENHANCEMENT.md](docs/ENHANCEMENT.md)** - AI 增强指南
- **[docs/UPLOAD_GUIDE.md](docs/UPLOAD_GUIDE.md)** - 如何将技能上传到 Claude
- **[docs/MCP_SETUP.md](docs/MCP_SETUP.md)** - MCP 集成设置
- **[docs/UNIFIED_SCRAPING.md](docs/UNIFIED_SCRAPING.md)** - 多源抓取指南

### 技术
- **[docs/CLAUDE.md](docs/CLAUDE.md)** - 技术架构
- **[STRUCTURE.md](STRUCTURE.md)** - 仓库结构
- **[README.md](README.md)** - 完整英文文档

---

## 总结

**一个工具完成所有任务:**
1. ✅ 抓取文档
2. ✅ 自动检测现有数据
3. ✅ 生成更好的知识
4. ✅ 创建增强的技能
5. ✅ 支持预设或自定义配置
6. ✅ 支持跳过抓取以快速迭代

**简单结构:**
- `src/skill_seekers/cli/` - 工具
- `configs/` - 预设
- `output/` - 所有输出

**更好的输出:**
- 真实代码示例,带语言检测
- 从文档中提取的常见模式
- 智能分类
- 增强的 SKILL.md,包含实际示例

---

## 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

---

## 贡献

欢迎贡献!请访问:
- GitHub 仓库: https://github.com/yusufkaraaslan/Skill_Seekers
- 问题跟踪: https://github.com/yusufkaraaslan/Skill_Seekers/issues
- 项目看板: https://github.com/users/yusufkaraaslan/projects/2

---

## 致谢

感谢所有贡献者和使用 Skill Seekers 的用户!

---

**准备好了吗?开始构建技能! 🚀**

```bash
# 尝试 Godot
skill-seekers scrape --config configs/godot.json

# 尝试 React
skill-seekers scrape --config configs/react.json

# 或使用交互模式
skill-seekers scrape --interactive
```

**祝你技能构建愉快!** 🎉
